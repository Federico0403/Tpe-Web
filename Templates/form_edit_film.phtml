<form action="<?= BASE_URL . 'editar/' . $film->id_peliculas ?>" method="POST">
    <div class="form-group">
        <label for="name_film">Nombre de la Película:</label>
        <input type="text" name="name_film" value="<?= htmlspecialchars($film->Nombre_pelicula) ?>" required>
    </div>
    <div class="form-group">
        <label for="date">Fecha de Estreno:</label>
        <input type="date" name="date" value="<?= htmlspecialchars($film->Lanzamiento) ?>" required>
    </div>
    <div class="form-group">
        <label for="director">Director:</label>
        <input type="text" name="director" value="<?= htmlspecialchars($film->director) ?>" required>
    </div>
    <div class="form-group">
        <label for="genre">Género:</label>
        <input type="text" name="genre" value="<?= htmlspecialchars($film->genero) ?>" required>
    </div>
    <div class="form-group">
        <label for="language">Idioma:</label>
        <input type="text" name="language" value="<?= htmlspecialchars($film->Idioma) ?>" required>
    </div>
    <div class="form-group">
        <label for="id_productoras">Productora:</label>
        <select name="id_productoras" required>
            <?php foreach ($producers as $producer): ?>
                <option value="<?= $producer->id_productoras ?>" <?= $film->id_productoras == $producer->id_productoras ? 'selected' : '' ?>>
                    <?= htmlspecialchars($producer->Nombre_productora) ?>
                </option>
            <?php endforeach; ?>
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Actualizar Película</button>
</form>
